# Exact runtime versions this project is demonstrated against (S8).
#
# Generated from a clean venv holding requirements.txt and nothing else, so
# this is the runtime closure rather than a developer's machine. The
# Dockerfile installs from here, which is what makes the image on demo
# morning the same image that was tested — unpinned '>=' resolved to
# whatever was newest that day, and that is how the audited tree drifted
# out from under the audit in the first place.
#
# Regenerate with: make lock     Audit with: make audit
# pip-audit against this set: No known vulnerabilities found.
#
# `pip` is deliberately absent. network-as-code==10.0.0 declares `pip==25.3`
# as a runtime dependency, which is a vendor packaging mistake: the SDK never
# imports pip, and it imports and resolves NetworkAsCodeApi with pip removed.
# pip 25.3 carries six advisories (PYSEC-2026-196, -1796, -2875, -2876,
# -3721), so shipping it to satisfy a dependency nothing uses would be a
# regression. The Dockerfile strips pip from the venv after installing.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
cffi==2.1.1
click==8.5.0
cryptography==50.0.1
dnspython==2.8.0
email-validator==2.3.0
fastapi==0.141.1
filelock==3.32.4
h11==0.16.0
httpcore==1.0.9
httpcore2==2.12.0
httptools==0.8.0
httpx==0.28.1
httpx2==2.12.0
idna==3.19
network_as_code==10.0.0
pycparser==3.0
pydantic==2.13.5
pydantic-settings==2.15.0
pydantic_core==2.46.5
PyJWT==2.13.0
python-dotenv==1.2.3
PyYAML==6.0.3
segno==1.6.6
sniffio==1.3.1
SQLAlchemy==2.0.52
starlette==1.6.0
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.4
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1
