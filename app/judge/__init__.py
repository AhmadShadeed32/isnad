"""The judge-facing paired evidence demonstration.

Two pieces: `access` holds the scoped session and every allowance a real Nokia
call has to pass, and `evidence` turns a validated source choice into a
provider instance with honest provenance.
"""
