# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
.venv311/
venv/
env/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage

# Environment / secrets. Never commit a real .env, signing key or database.
.env
.env.*
!.env.example
*.local
*.pem
*.key

# Local database and vault state
*.db
*.db-wal
*.db-shm
*.db.*
.isnad/

# OS / editor
.DS_Store
.idea/
.vscode/

# Working material kept on disk but out of the submission: agent instructions,
# the local knowledge graph, and the internal engineering notes and review
# records that predate the public README.
.claude/
CLAUDE.md
graphify-out/
docs/_internal/

# Video production. The finished submission video lives in submission/;
# the pipeline that renders it does not belong in an application repository.
video-build/
*.webm
audio/
